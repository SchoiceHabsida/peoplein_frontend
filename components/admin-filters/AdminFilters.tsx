export const AdminFilters = () => {
    return <div>
        <select className="outline-none select-bordered select select-sm max-w-xs">
            <option>Filter</option>
        </select>
    </div>
}